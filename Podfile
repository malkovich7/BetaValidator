platform :ios, '9.0'
plugin 'cocoapods-art', :sources => [
    'artifactory'
]

target 'BetaValidator' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BetaValidator
  pod 'SwiftyJSON', '~> 4.0'

end
